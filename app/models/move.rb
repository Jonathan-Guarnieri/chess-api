class Move < ApplicationRecord
  # os moves serao armazenados em um banco de dados como na notacao oficial
  # de xadrez (PGN), ou seja, a notacao algébrica. Exemplo: e2-e4, e7-e5, etc.
  # o mesmo vale para os movimentos especiais, como roque e en passant
end
